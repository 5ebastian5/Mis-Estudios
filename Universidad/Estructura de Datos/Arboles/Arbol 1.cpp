#include<iostream>
#include<conio.h>
#include<stdlib.h>
//ARBOLES BINARIOS

/* Mostrar arbol en orden (IZQUIERDA, NODO, DERECHA, REGRESO.)
*/
using namespace std;

struct nodo{
	int numero;
	nodo *izquierda, *derecha;	
};

void enlazar

main(){
	
	int menu;
	do{
		cout<<"\nOpcion: ";cin>>menu;
		switch(menu){
			
				}while(menu!=8);
}
